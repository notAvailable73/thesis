# Step 9 — MiniImageNet + MobileNetV3-Small + PARALLEL in-block placement (the
# Step-6/Step-8 winner: Pareto-dominant on both CIFAR-FS and MobileNetV3) +
# evidential head. Same placement sites as Step 8's mbnet/parallel
# (features.{3,6,8,11}, derived not hardcoded -- see
# src/backbones/mobilenetv3.py:mobilenetv3_stage_paths).
extends: exp_phase5_mini_mbnet_postpool_evidential.yaml

adapter:
  type: bottleneck
  rank: 16
  placement: parallel
  block_ids: [0, 1, 2, 3]

output:
  run_tag: mini_mbnet_parallel

wandb:
  tags: [phase5, step9, mini_imagenet, mobilenetv3_small, bottleneck,
         parallel, evidential, prototype, ravi_larochelle]
