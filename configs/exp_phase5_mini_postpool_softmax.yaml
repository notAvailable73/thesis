# Step 9 — MiniImageNet + ResNet-18 + POST-POOL Bottleneck + softmax head.
# Softmax sibling of exp_phase5_mini_postpool_evidential.
extends: exp_phase5_mini_base_softmax.yaml

adapter:
  type: bottleneck
  rank: 16
  placement: post_pool

output:
  run_tag: mini_postpool

wandb:
  tags: [phase5, step9, mini_imagenet, bottleneck, post_pool, softmax,
         prototype, ravi_larochelle]
