# Step 6 (RQ1) — Bottleneck adapter placed SERIAL + softmax prototype head.
# Softmax sibling of exp_phase3_placement_serial_evidential; same 1x1 bottleneck
# form / rank / block_ids so the only difference vs the evidential run is the
# head interpretation. See step_writeups/step6.txt.
extends: exp_phase2_softmax.yaml

adapter:
  type: bottleneck
  rank: 16
  placement: serial
  block_ids: [0, 1, 2, 3]

wandb:
  tags: [phase3, step6, placement, serial, softmax, prototype, cifar_fs_bertinetto]
