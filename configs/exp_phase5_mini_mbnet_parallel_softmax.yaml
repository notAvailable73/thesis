# Step 9 — MiniImageNet + MobileNetV3-Small + PARALLEL in-block placement +
# softmax head. Softmax sibling of exp_phase5_mini_mbnet_parallel_evidential.
extends: exp_phase5_mini_mbnet_postpool_softmax.yaml

adapter:
  type: bottleneck
  rank: 16
  placement: parallel
  block_ids: [0, 1, 2, 3]

output:
  run_tag: mini_mbnet_parallel

wandb:
  tags: [phase5, step9, mini_imagenet, mobilenetv3_small, bottleneck,
         parallel, softmax, prototype, ravi_larochelle]
