================================================================================
PROGRESS.TXT
Project:   B-PEFT (Bayesian Parameter-Efficient Fine-Tuning) thesis
Tracks:    the 13 steps of implementation.txt — one section per step,
           one checkbox per action item, exit criteria at the end.
Read order: proposal.txt → plan.txt → implementation.txt → this file.
Status key: [x] DONE   [~] IN PROGRESS   [ ] NOT STARTED
Updated:   2026-07-30 (Step 9 CLOSED — run completed, results transcribed)
================================================================================


--------------------------------------------------------------------------------
CURRENT STATUS  (read this first)
--------------------------------------------------------------------------------

Done:        Steps 1, 2, 3 (Phase 1); Step 4 pipeline (Phase 2, negative result);
             Step 4.5 "settle the science" (Phase 2.5, CLOSED FINAL — TIER 3,
             fully rigorous: real TinyImageNet near-OOD + VAL-swept evidential
             config, no open caveats; see below)
Just closed: Step 9 (Phase 5: MiniImageNet as a 2nd in-distribution dataset,
             RQ2/RQ3 breadth) — 10 configs trained + evaluated on Kaggle T4
             (2026-07-30, log timestamps 00:54:55-03:17 UTC, ~142 min), N=600,
             no collapse; results/step9_mini_artifacts/results/phase5_mini*_
             metrics.json (10) + phase5_dataset_table.json +
             step9_dataset_comparison.png + step9_adapter_uplift.png; pytest
             208 passed / 6 failed (214 collected — all 6 failures traced to
             one environment root cause, a real Kaggle dataset attached under
             /kaggle/input during the test run, zero are logic bugs — see
             step9.txt §5); step9.txt §4 filled and §4.3's five questions
             answered. Headline below. Byte-identical rerun spot-check STILL
             NOT DONE (optional, carried over from Step 8 too).
             (Step 8 closed just before — MobileNetV3-Small backbone, RQ4 prep;
             Step 7 — RQ3 far-vs-near OOD; Step 6 — RQ1 placement; Step 5 —
             PEFT baselines.)
Next:        Step 10 (MVT grid execution — all 4 RQs, the thesis's central
             results table). Three carry-overs into Step 10's grid:
             (i) per-backbone/per-dataset VAL tuning — the whole recipe (LR,
             rank, KL schedule, evidence affine) was inherited from a single
             ResNet-18/CIFAR-FS sweep and never re-tuned for MobileNetV3 OR
             MiniImageNet; (ii) the mbnet/parallel evidential cell's
             checkpoint-selected-during-KL-warm-up issue (Step 8: epoch 3,
             kl_w=0.030) REPRODUCED on MiniImageNet too (Step 9: epoch 6,
             kl_w=0.060) — a 2-for-2 pattern across independent datasets, now
             a higher-priority target for a longer warm-up / patience>5 test;
             (iii) NEW from Step 9: the MobileNetV3-vs-ResNet-18 accuracy gap
             that was only 0.3-1.6 pp on CIFAR-FS (Step 8) balloons to
             4.7-5.7 pp on MiniImageNet — untested whether this is a
             backbone-capacity effect or just the same untuned-recipe problem
             (i) hitting MobileNetV3 harder on this dataset; Step 10's
             per-cell VAL tuning is what will distinguish them.
Blocker:     none.
Today:       2026-08-02

Step 4 headline (real CIFAR-FS Bertinetto, N=600) — the R1 claim did NOT survive:
  evidential:  acc 0.870   far-OOD SVHN AUROC 0.843   ECE 0.344   Brier 0.353
  softmax:     acc 0.875   far-OOD SVHN AUROC 0.838   ECE 0.119   Brier 0.190
  AUROC gap +0.006 (< 0.05 bar); evidential WORSE calibrated. Escalation fired.

Step 4.5 FINAL headline (same split/protocol, N=600, R-EDL loss retune
VAL-selected via a real sweep, TS/energy baselines, near-OOD = REAL
TinyImageNet + CIFAR-100-heldout) — TIER 3, but evidential decisively beats
BOTH probability-based softmax scores (MSP, TS-MSP) on EVERY OOD pool tested:
  evidential:  acc 0.884 (+0.014 vs Step 4)   ECE 0.285 (-0.056)   Brier 0.285 (-0.068)
               far-OOD(SVHN) AUROC 0.914   near-OOD(CIFAR100) 0.878   near-OOD(TinyImageNet) 0.913
  softmax:     acc 0.875 (byte-identical across all 3 runs)   ECE 0.082 / TS 0.041
               far-OOD:  msp 0.838, ts_msp 0.828, energy 0.905
               near-OOD(TIN): msp 0.790, ts_msp 0.782, energy 0.925
  vs MSP/TS-MSP: evidential wins EVERY pool by +0.076 to +0.141 AUROC (decisive).
  vs ENERGY: evidential wins far-OOD (+0.009) and CIFAR100-near (+0.015), but
  LOSES TinyImageNet-near by -0.013 — the one near-miss keeping this at Tier 3.
  VAL sweep (real, in-process, 3 combos) found the ECE surface FLAT (~0.285-
  0.296) across kl_weight_max ∈ {0.05,0.1,0.25} — calibration ceiling confirmed,
  not assumed. Recommended narrative: evidential has a genuine, repeated
  OOD-detection edge over every confidence-based score, is competitive with
  (not clearly better than) the strongest non-probabilistic baseline (energy),
  and calibration remains the open problem even after a real search. See
  step_writeups/step4_5.txt §4, §4.1, §6, §7 for full numbers/story — this
  writeup is FINAL, no caveats remain open (§5, §8).
  Perf note: TinyImageNet loader initially hung 1h+ on Google Drive (120k-file
  extraction over FUSE) — fixed to read straight from the zip, no extraction.

Step 5 headline (Phase 3, same split/protocol, N=600, 2026-07-21) — TWO findings:
  (1) The evidential OOD edge GENERALISES across PEFT methods. Evidential vacuity
      beats softmax msp AND ts_msp on every OOD pool for all four trained-adapter
      methods (LoRA, BitFit, Full-FT, + Bottleneck ref) by +0.010 to +0.134 AUROC;
      vs energy it stays competitive-not-dominant (same as Step 4.5). The central
      claim is NOT a Bottleneck artefact. Calibration stays the open problem
      (evidential ECE ~0.33 vs softmax-TS 0.005-0.041).
  (2) Full-FT did NOT collapse — it is the TOP-accuracy run (softmax 0.905),
      contradicting proposal §6's "overfits on 25 images". Cause is a PROTOCOL
      mismatch (episodic meta-training freezes a shared backbone at test; there
      is no per-episode fine-tune to overfit), NOT a data leak (disjoint 64/20
      classes, BN frozen, early-stopped at ep 6). REFRAME §6: PEFT reaches ~98%
      of Full-FT accuracy (Bottleneck 0.884 / BitFit 0.881 vs 0.905) at 660x-
      2330x fewer params — the story is EFFICIENCY, not Full-FT viability.
  Accuracy: FullFT-sm .905 > FullFT-ev .888 > Bottleneck-ev .884 > BitFit-sm .881
      > Bottleneck-sm .875 > LinearProbe .874 > BitFit-ev .869 > LoRA-sm .860 >
      LoRA-ev .828. LoRA is weakest (single-1x1-conv capacity, pre-flagged).
      Linear-Probe evidential is the degenerate corner (ECE 0.623; vacuity
      collapses to 0.708/0.584 on svhn/cifar100 — no adapter to shape evidence).
  Full numbers/story: step_writeups/step5.txt §4.

Step 6 headline (Phase 3, RQ1, same split/protocol, N=600, 2026-07-21) — PLACEMENT
MATTERS, PARALLEL IS THE PARETO PICK:
  In-block placement of the 1x1 Bottleneck (serial/parallel, 31.7K params) beats
  post_pool (16.9K) by +3-4pp accuracy (0.913-0.915 vs 0.875-0.884) — and edges
  Step-5 Full-FT (0.905, 11.18M) at 0.3% of its trainable params. Serial vs
  parallel TIE on accuracy (0.9145 vs 0.9146 evid); the tiebreak is OOD:
    parallel-evid vacuity: svhn 0.933  cifar100 0.912  tin 0.929   (best OOD)
    serial-evid   vacuity: svhn 0.889  cifar100 0.904  tin 0.929   (svhn dips below post_pool 0.914)
  => parallel >= serial on every evidential OOD pool — Conv-Adapter's
  "parallel > sequential" replicates, concentrated in far-OOD. Evidential vacuity
  still beats softmax msp on EVERY pool+placement (+0.020..+0.098). Evidential ECE
  ~0.28 stays the open calibration problem. The 1x1 "loss of locality" caveat did
  NOT bite (in-block beat post_pool on accuracy). All 4 runs early-stopped normally
  (best_val_epoch 4-19) — 0.915 is genuine, not overfit.
  Full numbers/story: step_writeups/step6.txt §4 ; plot results/step6_placement_comparison.png.

Step 7 headline (Phase 4, RQ3, parallel winner, N=600, 2026-07-22) — EVIDENTIAL'S
OOD EDGE IS LARGER ON NEAR-OOD THAN FAR-OOD (the RQ3 point):
  Evidential vacuity beats softmax MSP on EVERY pool; gap widens near > far:
    far : svhn +0.045 (0.933 vs 0.888)   gaussian +0.048 (0.980 vs 0.932)
    near: cifar100 +0.098 (0.912 vs 0.814)   tin +0.080 (0.929 vs 0.848)
  FPR@95 same story (near cifar100: evid 0.393 vs softmax 0.667). Mechanism:
  MSP degrades under semantic (near) shift where the head stays confident;
  Dirichlet vacuity stays discriminative (OpenOOD near>far; Malinin & Gales).
  Gaussian sanity PASSES — both heads near-ceiling on white noise (evid 0.980;
  softmax msp 0.932, energy 0.984). Softmax energy stays competitive on far-OOD
  but loses on the decisive near-OOD axis. svhn/cifar/tin reproduced Step 6's
  parallel numbers EXACTLY (determinism verified). TinyImageNet still uncurated —
  near-OOD claim rests on the clean, disjoint cifar100_near.
  Full numbers/story: step_writeups/step7.txt §4 ; table results/phase4_ood_table.json.

Step 8 headline (Phase 5, RQ4 prep, MobileNetV3-Small vs ResNet-18, N=600,
2026-07-26) — THE SMALL BACKBONE IS ALMOST FREE, AND PARALLEL BECOMES PARETO-
DOMINANT:
  Cost of the swap at matched placement (mbnet − r18 accuracy):
    post_pool  evid −0.0163   softmax −0.0027      parallel evid −0.0098
                                                   parallel softmax −0.0035
  The two softmax gaps are INSIDE the 600-episode CI95 (±0.005); the two
  evidential gaps exceed it. Either way 0.3-1.6 pp, NOT the ~7-8 pp that
  MobileNetV3-Small's ImageNet top-1 deficit predicted — for a 12.1x smaller
  frozen trunk (927,008 params vs 11,176,512). Few-shot 5-way transfer does not
  need most of the capacity the ImageNet gap measures.
  RQ1 STRENGTHENS: within MobileNetV3, in-block parallel beats post_pool by
  +0.0376 (evid 0.9048 vs 0.8672) / +0.0370 (softmax 0.9090 vs 0.8720) while
  using 2.75x FEWER params (6,930 vs 19,026). On ResNet-18 the same +3-4 pp win
  cost 1.88x MORE params. So here parallel is strictly Pareto-dominant — better
  accuracy AND cheaper, no trade-off to argue. Conv-Adapter's 1x1
  "loss of locality" caveat did NOT bite in the narrow 24-96 ch stages.
  RQ4 PREP: mbnet/parallel (6,928 params, 927K trunk) reaches 0.9090, ABOVE
  Step-5 ResNet-18 Full-FT (0.9047, 11,176,512 params) — full-FT accuracy with
  1,613x fewer trained params — and within 0.35 pp of the thesis best
  (r18/parallel 0.9125 @ 31,744). Every mbnet/post_pool point is dominated.
  LATENCY/FLOPs NOT MEASURED — parameters only; that is Step 11.
  RQ2/RQ3 SURVIVE THE SWAP, WITH ONE REGRESSION:
    vacuity beats msp on all 8 cells (parallel: svhn +0.108, gaussian +0.015,
    cifar100 +0.041, tin +0.028) and ts_msp on all 8 (+0.033..+0.170).
    mbnet/parallel evidential sets the best far-OOD SVHN AUROC in the thesis
    (0.9522 vs r18/parallel 0.9328, FPR@95 0.226 vs 0.380) but its near-OOD is
    BELOW r18/parallel (cifar100 0.875 vs 0.912; tin 0.913 vs 0.929).
    REGRESSION vs ENERGY: energy now wins 3 of 4 pools on both mbnet placements
    (parallel: svhn +0.004 to vacuity, gaussian −0.048, cifar100 −0.013, tin
    −0.036). On ResNet-18 it was near-even (2-2). "Vacuity is on par with energy"
    is a ResNet-18 statement only — do not generalise it.
  CALIBRATION still the open problem, gap WIDER at the winning cell: evid ECE
  0.3069 vs TS-softmax 0.0088 = 35x (r18/parallel 16.6x). Most of the widening is
  the softmax side improving — 0.0088 is the best ECE_ts in the thesis. The
  0.3069 is the worst evidential ECE; suspect is that its checkpoint was selected
  at epoch 3, DURING the KL warm-up (kl_w=0.030 of 0.100), unlike the other three
  evidential runs. Untested — Step 10.
  No collapse anywhere (guard 0.25 never fired; mean_ev 1.69-2.56), so the Step-4
  evidential collapse did not recur at 576-d. All 4 early-stopped normally
  (best_val_epoch 14 / 1 / 3 / 10).
  Full numbers/story: step_writeups/step8.txt §4 ; table
  results/phase5_backbone_table.json ; plots results/step8_backbone_comparison.png
  + results/step8_params_vs_accuracy.png.

Step 9 headline (Phase 5, RQ2/RQ3 breadth, MiniImageNet vs CIFAR-FS, N=600,
2026-07-30) — THE PRETRAINING-OVERLAP CONFOUND IS REAL AND MEASURED, THE
EVIDENTIAL-VS-ENERGY EDGE ERODES FURTHER, AND THE BACKBONE-COST FINDING
DOES NOT REPLICATE:
  MiniImageNet accuracy is 4-9 pp higher than the matched CIFAR-FS cell for 9
  of 10 (adapter, head) pairs — mini/r18/parallel evidential peaks at 0.9601,
  the highest accuracy recorded anywhere in this thesis. The linear-probe
  control MEASURES the ImageNet-pretraining-overlap confound directly: the
  parallel-minus-linear-probe uplift shrinks from CIFAR-FS's +3.84 pp
  (softmax) / +4.05 pp (evidential) to MiniImageNet's +1.39 pp / +1.90 pp —
  a third to a half the CIFAR-FS value (corrects an earlier approximate
  "+4.1 pp" CIFAR-FS figure carried in instructions.txt/step9.txt — the
  real committed number is +3.84 pp). One cell (mbnet/parallel/softmax)
  inverts the pattern (-1.10 pp on MiniImageNet vs CIFAR-FS), reported
  as-is, not explained away.
  RQ2/RQ3: evidential vacuity beats softmax MSP and TS-MSP on all 16 of 16
  (pool, config) MiniImageNet comparisons — the probabilistic claim survives
  both the Step 8 backbone swap and this dataset swap intact. Against
  ENERGY specifically, the "near-even" CIFAR-FS story does NOT hold: vacuity
  wins only 7 of 16, and even r18/post_pool (a CIFAR-FS win for evidential)
  flips to a near-total loss (1/4) on MiniImageNet. CALIBRATION: evidential
  ECE stays flat (0.267-0.318, same band as CIFAR-FS's 0.264-0.307) while
  TS-softmax ECE drops to new thesis-wide lows (0.0047-0.0053 on ResNet-18)
  — the evidential-vs-TS-softmax gap explodes to 55-57x on ResNet-18 (vs
  Step 8's previous worst, 35x), the strongest evidence yet that the ECE
  surface really is flat regardless of dataset/backbone (Step 4.5's VAL
  sweep finding).
  RQ1/RQ4: placement (parallel > post_pool) REPLICATES on both backbones but
  the margin shrinks to +0.7-1.4 pp (vs CIFAR-FS's +3.1-3.8 pp) — a plausible
  near-saturation ceiling effect. The backbone-cost finding does NOT
  replicate: Step 8 found MobileNetV3 costs only 0.3-1.6 pp vs ResNet-18 on
  CIFAR-FS; on MiniImageNet the same gap is 4.7-5.7 pp (2.9-19x larger,
  outside CI95). Untested whether this is a MobileNetV3-capacity effect or
  the untuned-recipe caveat hitting harder here — Step 10's job to isolate.
  mbnet/parallel evidential's checkpoint-selected-during-KL-warm-up issue
  (Step 8: epoch 3/30, kl_w=0.030, worst evidential ECE) REPRODUCED exactly
  on MiniImageNet (epoch 6/30, kl_w=0.060, again the worst evidential ECE of
  its dataset, 0.3176) — 2-for-2 across independent datasets, raising the
  priority of testing a longer KL warm-up / patience>5 on this cell in
  Step 10.
  No collapse anywhere (guard 0.25 never fired). pytest: 208 passed / 6
  failed (214 collected) — all 6 traced to one environment cause (a real
  Kaggle dataset attached under /kaggle/input during the test run shadows
  tests that assume nothing is staged); zero are logic bugs in Step 9 code.
  1 of the 6 (test_exclude_wnids_drops_the_named_classes) is new to Step 9
  and, while explained by code inspection, is NOT independently confirmed by
  a logged count from the real run — see step9.txt §5 for the full trace.
  Full numbers/story: step_writeups/step9.txt §4 ; table
  results/step9_mini_artifacts/results/phase5_dataset_table.json ; plots
  results/step9_mini_artifacts/results/step9_dataset_comparison.png +
  step9_adapter_uplift.png.

Phase 1 stand-in numbers (Step 3, kept for reference; easier setup):
  evidential softplus_kl0.5:  acc 0.849 ± 0.006   AUROC 0.951
  softmax baseline:           acc 0.823 ± 0.006   AUROC 0.850


================================================================================
PHASE 1 — FOUNDATION  (CLOSED)
================================================================================

--------------------------------------------------------------------------------
STEP 1 — CALIBRATION FIX                                        [x] CLOSED
--------------------------------------------------------------------------------
Phase: 1     RQ: RQ2 (preliminary)     Closed: 2026-04-21
Writeup: step_writeups/step1.txt

Actions:
  [x] 1.1 Diagnose pre-defence demo's ECE=0.526 (two bugs identified:
          CE-on-softplus instead of evidential MSE+KL; KL prior disabled)
  [x] 1.2 Implement Sensoy 2018 evidential MSE + KL loss correctly in
          src/losses/evidential.py
  [x] 1.3 KL anneal 0 → 0.5 over 200 training steps in the trainer
  [x] 1.4 softplus activation (replaces ReLU; fixes dead-neuron issue —
          this is a documented deviation from proposal §5B)
  [x] 1.5 20-episode evaluation harness (fixed seeds [0..19])
  [x] 1.6 R1 trade-off story validated: AUROC +14pp, calibration cost
          small but non-zero
  [x] 1.7 Bug investigation + fix documented in step1.txt

Exit criteria:
  [x] evidential ECE down from 0.526 to 0.167 (order of magnitude
      improvement; the R1 framing is committed because strict ECE win
      against softmax was not achievable on this benchmark)
  [x] accuracy parity within 5 pp
  [x] AUROC(evidential) > AUROC(softmax) by a wide margin
  [x] reliability_plot.png shows evidential closer to the diagonal

--------------------------------------------------------------------------------
STEP 2 — CLEAN CONFIG-DRIVEN REPO                               [x] CLOSED
--------------------------------------------------------------------------------
Phase: 1     RQ: — (infrastructure)     Closed: 2026-05-13
Writeup: step_writeups/step2.txt

Actions:
  [x] 2.1 src/ reorganised into subpackages: adapters/, backbones/,
          datasets/, evaluators/, heads/, losses/, models/, trainers/,
          utils/
  [x] 2.2 YAML config system with `extends: <other.yaml>` + recursive
          deep-merge (src/utils/config.py)
  [x] 2.3 configs/base.yaml as the default; configs/exp_step1.yaml
          and configs/exp_step1_softmax.yaml as per-experiment overrides
  [x] 2.4 scripts/train.py and scripts/evaluate.py as the only CLI
          entry points (no model-specific imports at script top)
  [x] 2.5 28 pytest smoke tests under tests/ covering adapter
          identity-at-init, evidential non-negativity, KL anneal
          monotonicity, config extends merge, episode sampler
          invariants, the calibration evaluator
  [x] 2.6 Step 1 reproduction through the CLI: every metric within
          spec tolerance vs Step 1 numbers
  [x] 2.7 step2.txt writeup

Exit criteria:
  [x] scripts/train.py + configs/exp_step1.yaml runs end-to-end
  [x] output matches Step 1 within +/- 1% accuracy, +/- 0.01 ECE
  [x] no file in src/ is unused; no placeholder files
  [x] pytest tests/ → 28 passed, exit 0

--------------------------------------------------------------------------------
STEP 3 — WANDB + REPRODUCIBILITY                                [x] CLOSED
--------------------------------------------------------------------------------
Phase: 1     RQ: — (infrastructure)     Closed: 2026-05-16
Writeup: step_writeups/step3.txt

Actions:
  [x] 3.1 src/utils/wandb_utils.py — WandbRun wrapper, three first-class
          modes (online / offline / disabled), run-name format from spec
  [x] 3.2 Per-step W&B logging in src/trainers/fewshot_trainer.py: loss,
          support_acc, lr, kl_weight (evidential only)
  [x] 3.3 Per-episode W&B logging in scripts/evaluate.py: accuracy,
          ECE, Brier, OOD AUROC + four artifacts (reliability / OOD
          histogram / confusion matrix / metrics JSON)
  [x] 3.4 src/utils/seed.py — deterministic seeding by default
          (cudnn.deterministic, cublas workspace, python+numpy+torch
          CPU+CUDA RNGs)
  [x] 3.5 wandb.disabled flag honoured by base.yaml
  [x] 3.6 configs/test_episodes.yaml — canonical 600 test-episode seeds
          [0..599]; comment block reads "DO NOT REGENERATE — frozen"
  [x] 3.7 scripts/evaluate.py reads the 600-seed list at startup;
          metrics JSON records seeds_first10 + seeds_last10 for audit
  [x] 3.8 src/utils/plots.py — reliability / OOD histogram / confusion
          matrix lifted out of the Step 1 notebook into reusable funcs
  [x] 3.9 step3.txt writeup

Exit criteria:
  [x] W&B dashboard has 4 runs (train + eval × 2 configs). Ran in
      offline mode because the embedded API key was 36 chars; wandb
      requires 40+. The `wandb/` directory is ready to `wandb sync`
      once an online key is available.
  [x] Running the same config twice gives byte-identical metrics.json
      (filecmp.cmp asserted inside the notebook)
  [x] Same 600 test episodes used regardless of who runs evaluate
  [x] pytest tests/ → 28 passed, exit 0 (unchanged from Step 2)


================================================================================
PHASE 2 — REAL BENCHMARK + EPISODIC PROTOCOL  (NEXT)
================================================================================

--------------------------------------------------------------------------------
STEP 4 — CIFAR-FS BERTINETTO SPLIT + EPISODIC META-TRAINING     [ ] NOT STARTED
--------------------------------------------------------------------------------
Phase: 2     RQ: foundation for RQ2 + RQ3 on real benchmark    Est: 1-2 weeks

Pre-flight checks (run before starting):
  [ ] pytest tests/ → 28 passed
  [ ] `python scripts/train.py --config configs/exp_step1.yaml` runs
  [ ] `python scripts/evaluate.py --config configs/exp_step1.yaml \
       --num-episodes 600 --wandb-mode disabled` matches existing
       results/step3_*_metrics.json on every metric
  [ ] configs/test_episodes.yaml present with 600 seeds [0..599]

Protocol decisions (committed in plan.txt §4 Phase 2 + implementation.txt §4):
  [x] A. Episodic meta-training (ProtoNet-style; one outer step per
         episode; parameter-free prototype head)
  [x] B. 5-shot only for Phase 2 (1-shot deferred to Step 10's MVT grid)
  [x] C. Documented architectural deviation: linear head → prototype
         head for meta-training (justified by standard CIFAR-FS protocol)

Actions:
  [ ] 4.1  data/cifar_fs_split.json — Bertinetto 2019 canonical 64/16/20
           class IDs; comment "DO NOT REGENERATE — frozen"
  [ ] 4.2  src/datasets/cifar_fs.py — get_cifar_fs(split: str, ...);
           filter CIFAR-100 to the split's class IDs; re-index labels
           to contiguous range per split; disjoint-split assertion at
           import
  [ ] 4.3  src/datasets/episode_sampler.py — EpisodicIterableDataset
           wrapper for streaming train + val episodes (memoised
           label→indices map; per-episode deterministic seed)
  [ ] 4.4  src/heads/prototype_head.py — parameter-free L2 / cosine
           prototype similarity classifier
  [ ] 4.5  src/models/bpeft_model.py — add "prototype" head_type
           alongside "linear"; linear stays for the fallback / legacy
  [ ] 4.6  src/trainers/episodic_trainer.py — outer epoch loop, per-
           episode forward + outer step, per-epoch validation on val
           split, early stop on val accuracy plateau, KL anneal over
           total outer steps
  [ ] 4.7  src/evaluators/calibration.py + src/evaluators/ood.py —
           evaluate_episodic() with the Step 3 schema PLUS
           f1_macro_mean, f1_macro_std, f1_macro_ci95,
           fpr_at_95_tpr, best_val_epoch
  [ ] 4.7.1 src/evaluators/accuracy.py — add f1_macro() function
            (Macro-F1 was the proposal §7 metric missing from Phase 1)
  [ ] 4.8  configs/val_episodes.yaml — 100 fixed val seeds
           [10000..10099]; comment "DO NOT REGENERATE — frozen"
  [ ] 4.9  configs/exp_phase2_evidential.yaml — episodic trainer
           settings (num_epochs=30, episodes_per_epoch=100,
           val_episodes_per_epoch=100, early_stop_patience=5,
           kl_anneal_steps=1000)
  [ ] 4.10 configs/exp_phase2_softmax.yaml — sibling config, softmax
           head, no KL block
  [ ] 4.11 configs/base.yaml — default trainer.type=episodic;
           head.classifier=prototype; old single_episode kept for
           legacy reproduction tests
  [ ] 4.12 scripts/train.py — branch on cfg.trainer.type; episodic
           path builds train + val iterables and calls
           EpisodicTrainer.fit()
  [ ] 4.13 scripts/evaluate.py — branch on cfg.trainer.type to choose
           evaluate_episodic() vs evaluate_finetune()
  [ ] 4.14 tests/test_cifar_fs_split.py — split disjoint; union = 0..99
  [ ] 4.15 tests/test_episodic_trainer.py — 1 epoch × 5 episodes smoke
           on synthetic data; val_acc > 1/n_way after epoch 1
  [ ] 4.16 tests/test_prototype_head.py — perfect prototype match wins
  [ ] 4.17 Run scripts/train.py + scripts/evaluate.py on both configs;
           600-episode eval on the new test split
  [ ] 4.18 step_writeups/step4.txt — same structure as step1/2/3.txt

Exit criteria (all must tick):
  [ ] data/cifar_fs_split.json committed; assert disjoint splits;
      union = 0..99
  [ ] `scripts/train.py --config configs/exp_phase2_evidential.yaml`
      runs end-to-end on Colab T4 in ≤ 30 min
  [ ] same for configs/exp_phase2_softmax.yaml
  [ ] results/phase2_bottleneck_evidential_metrics.json AND
      results/phase2_bottleneck_softmax_metrics.json exist; both
      record: accuracy_mean/ci95/std, f1_macro_mean,
      ece_per_episode_mean, ece_pooled, brier_mean, ood_auroc_mean,
      fpr_at_95_tpr, num_episodes=600, best_val_epoch
  [ ] R1 trade-off survives the real split: AUROC gap > 0.05.
      If it does NOT, escalate before Step 5
  [ ] pytest tests/ → 31+ passed (3 new tests added)
  [ ] same config twice → byte-identical metrics.json

Risks (re-read before starting):
  [ ] R-EPISODIC-COLLAPSE: smoke assertion in scripts/train.py aborts
      if val_acc < 0.30 after epoch 1
  [ ] R-FALLBACK: if meta-training fails, fall back to per-episode-
      fine-tune on the new Bertinetto split (uses evaluate_finetune)
  [ ] R-SPLIT-DRIFT: do NOT regenerate the split file


--------------------------------------------------------------------------------
STEP 4.5 — SETTLE THE SCIENCE (fair baselines + near-OOD verdict)   [x] CLOSED
--------------------------------------------------------------------------------
Phase: 2.5   RQ: RQ2 + RQ3 (decisive re-test)   Writeup: step_writeups/step4_5.txt
Spec/plan: docs/superpowers/{specs,plans}/2026-07-06-step4-5-settle-the-science*

Motivation: Step 4's negative result was measured on the WRONG axis (far-OOD)
without the baseline that most threatens the thesis (temperature scaling). This
sub-step adds the fair test before reframing.

Actions:
  [x] W0  real Bertinetto split materialized (scripts/build_cifar_fs_split.py;
          _status=canonical_bertinetto_via_torchmeta). R-SPLIT-DRIFT: reproducible via the
          regenerator script (data/ is gitignored so the file isn't tracked;
          `git add -f` it if you want it versioned)
  [x] W0  fixed buggy test_prototype_head_raises_on_missing_class
  [x] W1  temperature scaling (Guo) — src/evaluators/temperature.py + tests
  [x] W1  energy score (Liu) — src/evaluators/ood.py:energy_score + tests
  [x] W2  R-EDL loss knobs (prior_per_class, use_variance) threaded through
          loss/vacuity/trainer/train.py; configs/exp_phase2_evidential_retuned
  [x] W3  near-OOD loaders (TinyImageNet + CIFAR-100-heldout) + tests
  [x] W3  evaluate_episodic refactored to a score x OOD-pool matrix + tests
  [x] W3  scripts/evaluate.py wired (val-T fit, OOD pools, wider JSON)
  [x] W4  scripts/step45_verdict.py (master table + tiered rule) + tests
  [x] W4  notebooks/step4_5_settle.ipynb (Colab runner)
  [x] RUN step4_5_settle.ipynb on Colab -> results/step45_*_metrics.json
      (FIRST run 2026-07-06 with hand-picked evidential config + CIFAR-100-
      heldout near-OOD only; FINAL run 2026-07-07 with a real in-process
      VAL-only R-EDL sweep + real TinyImageNet near-OOD — this is the one the
      numbers below reflect)
  [x] apply the tiered verdict; fill step_writeups/step4_5.txt §4; decide narrative
  [x] W2-followup  scripts/step45_val_sweep.py (in-process VAL sweep; ran
      cleanly, kl_weight_max=0.05 won, calibration surface confirmed FLAT)
  [x] W3-followup  scripts/evaluate.py --use-tinyimagenet (eval-only, no
      retrain; ran cleanly after fixing a Drive-extraction perf bug — see below)

Exit criteria:
  [x] both step45 metrics JSONs exist with the matrix keys + temperature/ece_ts
      + tin_near columns
  [x] tier decided (1/2/3) via scripts/step45_verdict.py -> TIER 3 (final)
  [x] pytest green locally (66 passed, 0 skipped — TinyImageNet zip-read test
      now runs for real, no more skip-guard)
  [x] no open caveats (both follow-ups executed successfully; see step_writeups/
      step4_5.txt §5, §8)

Decision rule (step_writeups/step4_5.txt §3): T1 evidential beats MSP AND
TS-softmax on near-OOD by >=0.03 and isn't worse-calibrated; T2 ties near-OOD
but wins ID calibration; T3 reframe to parity.

RESULT (FINAL): Tier 3, but NOT a flat loss — evidential vacuity beats BOTH
softmax-MSP and TS-softmax on EVERY OOD pool (far-OOD SVHN + two independent
near-OOD sets: CIFAR-100-heldout AND real TinyImageNet), by +0.076 to +0.141
AUROC — decisive and repeated. Against the strong non-probabilistic ENERGY
score specifically, evidential wins far-OOD (+0.009) and CIFAR100-near
(+0.015) but LOSES TinyImageNet-near by -0.013 — the real near-miss that keeps
this at Tier 3 (short of the 0.03 "decisive win" bar). Calibration improved
over Step 4 (ECE -0.056, Brier -0.068) but a REAL VAL-only sweep confirmed the
surface is flat around ECE~0.285 — still ~7x worse than TS-softmax (0.041).
See step_writeups/step4_5.txt §4/§4.1/§6/§7 for the full table and narrative.
Both follow-ups (TinyImageNet near-OOD, VAL sweep) were built AND executed
successfully — no caveats remain open. One perf bug found+fixed en route:
TinyImageNet's ~120k-file archive extracted onto Google Drive hung 1h+ (FUSE
per-file round-trip); fixed to read images straight out of the zip.


================================================================================
PHASE 3 — ALL PEFT METHODS + PLACEMENT  (NOT STARTED)
================================================================================

--------------------------------------------------------------------------------
STEP 5 — LORA + BITFIT + NON-PEFT BASELINES                     [x] CLOSED
--------------------------------------------------------------------------------
Phase: 3     RQ: RQ1, RQ2 + proposal §6 baselines    Closed: 2026-07-21
Writeup: step_writeups/step5.txt (COMPLETE; results §4 filled from the run)

Actions:
  [x] 5.1  src/adapters/lora.py — REWRITTEN from Step 2 stub; LoRALayer +
           LoRAConv2d + LoRAAdapter (injects LoRAConv2d into the backbone's
           cfg.adapter.lora_targets; default layer4.0.downsample.0, 1x1 conv,
           12,288 params @ rank 16). No longer a pooled-feature adapter.
  [x] 5.2  src/adapters/bitfit.py — unfreezes backbone .bias params only
           (BN betas, ~4,800)
  [x] 5.3  src/adapters/full_ft.py — proposal §6 PRIMARY BASELINE
           (unfreezes the whole backbone, ~11.18M with fc removed)
  [x] 5.4  src/adapters/linear_probe.py — proposal §6 PRIMARY BASELINE
           (backbone frozen; only head trainable: 0 softmax / 2 evidential)
  [x] 5.5  src/models/bpeft_model.py + src/adapters/__init__.py — factory
           accepts adapter.type ∈ {bottleneck, lora, bitfit, full_ft,
           linear_probe}; build_adapter now takes the backbone; model carries a
           backbone_trainable flag; adapter_features is grad-aware. ALSO:
           episodic_trainer.py gained freeze_backbone + a collapse-guard gate
           (threshold<=0 disables it for Full-FT) + a generalized grad-norm;
           scripts/train.py plumbs freeze_backbone and handles the zero-param
           Linear-Probe path (train-free checkpoint).
  [x] 5.6  8 configs: configs/exp_phase3_{lora,bitfit,full_ft,
           linear_probe}_{evidential,softmax}.yaml (+ scripts/run_step5.sh)
  [x] 5.7  Ran all 8 configs through scripts/train + scripts/evaluate (Colab,
           2026-07-21, N=600)
  [x] 5.8  8 result JSONs present: results/phase3_<adapter>_<head>_metrics.json
  [x] 5.9  4 new tests: test_lora, test_bitfit, test_full_ft,
           test_linear_probe (+ test_adapters.py updated: old pooled-feature
           LoRA-stub tests removed)
  [x] 5.10 step_writeups/step5.txt — COMPLETE; results §4 tables filled +
           Full-FT proposal-§6 reframe written.

Exit criteria:
  [x] All 8 result JSONs present                                        DONE
  [x] Trainable param counts documented (step5.txt §2):
        Bottleneck rank-16:  16,912 (adapter; 19,477 was Step-1 linear head)
        LoRA rank-16:        12,288 (layer4.0.downsample.0)
        BitFit (all BN betas): 4,800
        Full-FT:             11,176,512 (fc removed; 11.7M with fc)
        Linear Probe:         0 (softmax) / 2 (evidential evidence-affine)
  [x] proposal §6 baselines numerically compared against PEFT methods   DONE (§4.3)
  [x] pytest tests/ → 70+ passed                                        DONE (91 passed)
  [ ] byte-identical rerun spot-check for one config                    PENDING (optional)

--------------------------------------------------------------------------------
STEP 6 — ADAPTER PLACEMENT STUDY (RQ1)                          [x] CLOSED
--------------------------------------------------------------------------------
Phase: 3     RQ: RQ1 (primary)                        Closed: 2026-07-21
Writeup: step_writeups/step6.txt (COMPLETE; results §4 + RQ1 paragraph filled)
Design/plan: docs/superpowers/specs|plans/2026-07-21-step6-adapter-placement*

Actions:
  [x] 6.1  src/adapters/placement.py — Conv1x1Bottleneck +
           register_serial_adapter / register_parallel_adapter (forward hooks
           on the final BasicBlock of each stage) + PlacementAdapter.
  [x] 6.2  Placement wired via build_adapter (adapter.type=bottleneck +
           adapter.placement ∈ {post_pool, serial, parallel}, block_ids).
           Reuses backbone_trainable=True (like LoRA); NO resnet18.py /
           trainer / evaluator changes needed.
  [x] 6.3  4 configs: configs/exp_phase3_placement_{serial,parallel}_
           {evidential,softmax}.yaml (rank 16, block_ids [0,1,2,3]).
  [x] 6.4  Ran + evaluated on Kaggle (T4, N=600); 4 result JSONs
           results/phase3_placement_*; all 4 OK (best_val_epoch 4-19).
  [x] 6.5  scripts/step6_placement_plot.py -> results/step6_placement_
           comparison.png (300 dpi; 3 placements × 2 heads).
  [x] 6.6  step_writeups/step6.txt with the RQ1 Pareto paragraph filled.
  [x] 6.7  Kaggle notebook notebooks/step6_placement.ipynb (git clone +
           attached bpeft-data dataset; resilient CIFAR staging).

Exit criteria:
  [x] 3 placement variants (post_pool reused + serial + parallel) × 2 heads
        = 6 result JSONs
  [x] Trainable param counts reported per placement (16,912 post_pool;
        31,744 serial/parallel; +2 for evidential)
  [x] RQ1 paragraph written: parallel is the Pareto pick (0.915 acc / 0.933
        far-OOD / 31.7K params; beats post_pool and Step-5 Full-FT); serial
        ties on accuracy but dips on far-OOD. See step6.txt §4.3.
  [x] pytest tests/ → 103 passed (test_placement.py: 12 new)
  [ ] byte-identical rerun spot-check for one config              PENDING (optional)


================================================================================
PHASE 4 — OOD BREADTH  (NOT STARTED)
================================================================================

--------------------------------------------------------------------------------
STEP 7 — NEAR-OOD TINYIMAGENET + FAR-OOD GAUSSIAN (RQ3)         [x] CLOSED
--------------------------------------------------------------------------------
Phase: 4     RQ: RQ3 (primary)                        Closed: 2026-07-22
Writeup: step_writeups/step7.txt (COMPLETE; §4 + RQ3 paragraph filled)
Design/plan: docs/superpowers/specs|plans/2026-07-22-step7-far-ood-gaussian*
Scope note: "tight consolidation" — near-OOD infra (7.1/7.3) already existed
  from Steps 4.5/5/6; Step 7 added the Gaussian far-OOD pool + consolidated the
  RQ3 story on the Step-6 winner (parallel). TinyImageNet kept UNCURATED
  (documented); RQ3 rests on the clean, disjoint cifar100_near.

Actions:
  [x] 7.1  TinyImageNet near-OOD loader ALREADY built (Step 4.5); reused as-is,
           uncurated (class-overlap filtering deferred — see caveat).
  [x] 7.2  src/datasets/gaussian_noise_ood.py — seeded N(0,1), clamp [-3,3],
           deterministic (torch.Generator). Exported in datasets/__init__.py.
  [x] 7.3  Multi-OOD AUROC + FPR@95 per pool ALREADY in evaluate_episodic
           (Step 4.5); --use-gaussian just adds a pool to the generic loop.
  [x] 7.4  Re-ran the parallel winner (both heads) with --use-tinyimagenet
           --use-gaussian; results/phase4_parallel_* + phase4_ood_table.json.
  [x] 7.5  scripts/step7_ood_consolidate.py -> phase4_ood_table.json +
           results/step7_ood_comparison.png (far-vs-near AUROC bars).
  [x] 7.6  step_writeups/step7.txt with the RQ3 paragraph filled.
  [x] 7.7  Kaggle notebook notebooks/step7_ood.ipynb.

Exit criteria:
  [x] AUROC + FPR@95 for 4 OOD datasets (svhn/gaussian/cifar100/tin) × both heads
  [x] RQ3 paragraph (honest finding): evidential beats softmax MSP on EVERY pool,
        by MORE on near-OOD (+0.08..+0.10) than far-OOD (+0.045..+0.048); Gaussian
        sanity passes. See step7.txt §4.3.
  [x] pytest tests/ → 107 passed (test_gaussian_ood.py: 4 new)
  [ ] byte-identical rerun spot-check for one config              PENDING (optional)


================================================================================
PHASE 5 — FULL GRID + BACKBONES + EDGE EFFICIENCY  (IN PROGRESS — Steps 8-9 CLOSED)
================================================================================

--------------------------------------------------------------------------------
STEP 8 — MOBILENETV3-SMALL BACKBONE                             [x] CLOSED
--------------------------------------------------------------------------------
Phase: 5     RQ: RQ4 (prep)     Closed: 2026-07-26
Writeup: step_writeups/step8.txt (COMPLETE — §4 filled from the run, §4.3 answered)
Scope: second mandatory backbone (proposal §5A) + re-run the Step-6 WINNER
  (parallel in-block 1x1 Bottleneck, both heads) on it, plus a post-pool
  Bottleneck row per head so placement can also be read WITHIN this backbone.
  4 configs / 4 result JSONs / 600 test episodes each. ResNet-18 comparison rows
  are REUSED from Steps 4.5 + 7 — nothing earlier is re-run or changed.

Actions:
  [x] 8.1  src/backbones/mobilenetv3.py — build_frozen_mobilenetv3_small
           (torchvision IMAGENET1K_V1, classifier=Identity, frozen, eval) ->
           (B, 576); MOBILENETV3_SMALL_FEATURE_DIM; mobilenetv3_stage_paths()
           derives the in-block placement sites by traversal (features.3/6/8/11
           = 24/40/48/96 ch — the stage-final RESIDUAL blocks, the only
           shape-preserving sites, so parallel composition is valid).
           backbones/__init__ gained build_backbone(name, pretrained=True) with
           aliases + BACKBONE_FEATURE_DIMS + backbone_feature_dim().
  [x] 8.2  Adapters verified at feature_dim=576 (tests/test_mobilenetv3.py):
           post-pool Bottleneck 19,024; serial/parallel placement 6,928;
           LoRA family default (features.11.block.3.0) 10,752; BitFit;
           Linear-Probe 0. placement.py is now backbone-agnostic
           (resolve_stage_paths + infer_block_channels + optional
           adapter.stage_paths override); lora.py gained
           default_lora_targets(backbone). BOTH refactors keep the ResNet-18
           path identical — 3 regression tests assert the same module objects,
           31,744 param count and unchanged LoRA target.
           build_model now asserts backbone.feature_dim matches the backbone.
  [x] 8.3  4 configs: configs/exp_phase5_mbnet_{bottleneck,parallel}_
           {evidential,softmax}.yaml. Each overrides ONLY backbone (+placement,
           +output.run_tag) on top of the Step-4.5 retuned / phase-2 softmax
           recipe, so the comparison isolates the backbone.
  [x] 8.3b scripts/train.py + evaluate.py: new optional cfg.output.run_tag in
           the checkpoint tag (all 4 configs share adapter.type=bottleneck and
           would otherwise overwrite each other's .pt). run_tag unset ->
           byte-identical filenames to Steps 1-7. train.py also logs the
           backbone + resolved placement sites. Metrics-JSON schema UNTOUCHED
           (adding a key would break every earlier step's byte-identical rerun).
  [x] 8.3c scripts/step8_backbone_compare.py -> results/phase5_backbone_table
           .json + step8_backbone_comparison.png + step8_params_vs_accuracy.png
           (RQ4-prep scatter incl. the Step-5 Full-FT point). Cells keyed by
           result FILENAME, not by JSON fields.
  [x] 8.4a Kaggle notebook notebooks/step8_mbnet.ipynb — dataset-protocol
           guidelines in the intro, env/data/split staging, tests, a backbone
           sanity cell, the 4 resumable runs (--use-tinyimagenet --use-gaussian),
           consolidation + plots, and an artifact-packaging cell that zips the 4
           CHECKPOINTS + all Step 8 results with a sha256 MANIFEST and prints a
           download link.
  [x] 8.4b RAN it: 4 train+eval pairs, Kaggle T4, 2026-07-26 11:45-12:29 UTC,
           ~44 min total. All 4 trained end-to-end and early-stopped on the
           val-acc plateau (best_val_epoch 14 / 1 / 3 / 10 of 30, patience 5);
           the collapse guard (0.25) never fired and evidential mean_ev stayed
           1.69-2.56, so the Step-4 evidential collapse did NOT recur at 576-d.
           Split reported canonical_bertinetto_via_torchmeta 64/16/20. SVHN was
           not in the attached dataset and downloaded at runtime (the fix in
           801e21e); TinyImageNet likewise. -> 4 result JSONs +
           phase5_backbone_table.json + the 2 plots. Artifacts landed in
           results/ (checkpoints -> gitignored checkpoints/, sha256 provenance
           in results/step8_mbnet_MANIFEST.txt).
  [x] 8.5  step_writeups/step8.txt §4 filled by transcription from
           results/phase5_backbone_table.json; §4.3's five questions answered;
           §5 caveats extended (single seed; warm-up-truncated evidential
           checkpoint; energy regression); §1 headline written.

Exit criteria:
  [x] MobileNetV3-Small runs full episodic trainer end-to-end             4/4 runs
  [x] Best (adapter, head) from Step 6 evaluated on MobileNetV3 ×
      CIFAR-FS with 600-episode CI      parallel: 0.9048 ±0.0052 evidential,
                                                  0.9090 ±0.0053 softmax
  [x] 4 result JSONs results/phase5_mbnet*_metrics.json
  [x] pytest tests/ passes with test_mobilenetv3.py added         135 passed on
      Kaggle T4 (107 before + 28 new, incl. the 3 ResNet-18 guards)
  [ ] byte-identical rerun spot-check for one config      NOT DONE (optional; no
      new nondeterminism was introduced, but that is an argument not a
      measurement — cheapest close is to re-run one evaluate.py cell against the
      saved checkpoint and diff the JSON)

--------------------------------------------------------------------------------
STEP 9 — MINIIMAGENET DATASET                                          [x] CLOSED
--------------------------------------------------------------------------------
Phase: 5     RQ: RQ2, RQ3 breadth     Est: 1 week     Closed: 2026-07-30

Actions:
  [x] 9.1  src/datasets/mini_imagenet.py:MINI_IMAGENET_SPLIT — frozen
           Ravi & Larochelle 64/16/20 wnid split, cross-checked byte-for-byte
           against a live fetch of the canonical CSVs during planning.
           scripts/build_mini_imagenet_split.py materializes
           data/mini_imagenet_split.json (gitignored, same convention as
           cifar_fs_split.json) and cross-validates against the frozen
           constant — RUN on Kaggle T4: disjoint asserted (test 20/train
           64/val 16 classes, all "OK").
  [x] 9.2  src/datasets/mini_imagenet.py — auto-discovers 3 staged source
           layouts (Zenodo pkl caches / images+csv / wnid-imagefolder) or
           downloads the Zenodo pkls (MD5-checked); resize 84→224 via the
           same transform every other loader uses. get_id_split /
           get_heldout_near_ood dispatch layer added to src/datasets/
           __init__.py, replacing 6 hardcoded get_cifar_fs(...) sites in
           scripts/train.py + evaluate.py + step45_val_sweep.py — verified
           byte-identical for cifar_fs configs (regression tests). The
           Zenodo pkl schema (_load_pkl_cache) decoded 3 real staged pkls
           correctly on the real run (see 9.3b).
  [x] 9.3  10 configs/exp_phase5_mini_*.yaml (2 backbones x 2 placements x
           2 heads + ResNet-18 linear-probe reference x 2 heads) —
           Phase-2-style episodic train/val/test, same recipe as the
           CIFAR-FS siblings. All 10 RAN end-to-end with distinct run_tag/
           result filenames (zero collisions).
  [x] 9.3b RUN notebooks/step9-mini(1).ipynb on Kaggle T4 — DONE 2026-07-30,
           log timestamps 00:54:55-03:17 UTC (~142 min), RUN STATUS 10/10 OK,
           no evidential run's collapse guard (0.25) fired. Artifacts (10
           result JSONs + phase5_dataset_table.json + 2 plots + a sha256
           MANIFEST; checkpoints listed in the manifest but NOT committed —
           checkpoints/ is gitignored) committed under
           results/step9_mini_artifacts/ (commit 2dd71ab "step 9 complete").
  [x] 9.4  results/step9_mini_artifacts/results/phase5_mini_<adapter>_
           <head>_metrics.json (10) — DONE.
  [x] 9.5  step_writeups/step9.txt — §4 transcribed VERBATIM from
           phase5_dataset_table.json (honesty rule respected: spot-checked
           field-for-field against the individual metrics JSONs and the
           reused CIFAR-FS reference JSONs), §4.3's five questions answered
           with numbers, §5/§6 updated post-run.
  Extras beyond the original 9.1-9.5 spec: TinyImageNet exclude_wnids
  (25 wnids shared with MiniImageNet, 5 of them MiniImageNet TEST classes —
  filtered only for mini_imagenet runs, byte-identical default for cifar_fs;
  the FILTERING LOGIC's real-run correctness rests on code inspection, not
  an independently logged exclusion count — see step9.txt §5);
  scripts/step9_dataset_compare.py (cross-dataset consolidation, mirrors
  step8_backbone_compare.py); 35 new tests (tests/test_mini_imagenet.py,
  test_dataset_dispatch.py, + test_tinyimagenet_ood.py extensions) — RUN:
  208 passed / 6 failed (214 collected) full-suite; all 6 failures trace to
  one environment cause (a real Kaggle dataset attached under /kaggle/input
  during the test run), zero are logic bugs — see step9.txt §5 for the full
  trace, including the one Step-9-authored failure
  (test_exclude_wnids_drops_the_named_classes).

Exit criteria:
  [x] MiniImageNet split JSON committed, disjoint asserted BY AN ACTUAL RUN
  [x] Bottleneck × {evidential, softmax} runs on MiniImageNet end-to-end —
      r18/post_pool 0.9459 evid / 0.9424 softmax, r18/parallel 0.9601 evid /
      0.9550 softmax (600 episodes each)
  [ ] (carried, optional) byte-identical rerun spot-check — STILL NOT DONE;
      also the standing item carried over from Step 8 (never closed there
      either). Requires a fresh Kaggle session or the pushed-checkpoint
      artifact, since checkpoints/ is gitignored — see step9.txt §5.

--------------------------------------------------------------------------------
STEP 10 — MVT GRID EXECUTION                                    [ ] NOT STARTED
--------------------------------------------------------------------------------
Phase: 5     RQ: all four                             Est: 2-3 weeks

Actions:
  [ ] 10.1 scripts/run_mvt_grid.sh — loops 120 runs (96 PEFT + 24
           baseline); skip on existing metrics JSON for resume
  [ ] 10.2 scripts/build_grid_configs.py + configs/grid/*.yaml
           (auto-generated, then committed)
  [ ] 10.3 scripts/aggregate_grid.py → results/mvt_results.json
           with schema
           {dataset}.{kshot}.{backbone}.{adapter}.{head}.{metric}
           = {mean, std, ci95}
  [ ] 10.4 Three master tables (accuracy, calibration, OOD AUROC) in
           LaTeX booktabs + 300 dpi PNG; baselines as separate
           subsection of the accuracy table
  [ ] 10.5 step_writeups/step10.txt

Exit criteria:
  [ ] All 96 PEFT cells + 24 baseline cells have a metrics JSON
  [ ] mvt_results.json complete; no missing cells
  [ ] Three master tables saved
  [ ] Wandb dashboard groups runs by (dataset, shots); baselines
      tagged "baseline"
  [ ] 16 plots (reliability + OOD histogram per dataset × head)
      at 300 dpi

--------------------------------------------------------------------------------
STEP 11 — EFFICIENCY + PARETO FRONTIER (RQ4)                    [ ] NOT STARTED
--------------------------------------------------------------------------------
Phase: 5     RQ: RQ4 (primary)                        Est: 1 week

Actions:
  [ ] 11.1 src/utils/efficiency.py — params (trainable + total), FLOPs
           (fvcore), latency (T4 + Colab CPU), peak memory
  [ ] 11.2 scripts/efficiency_table.py → results/efficiency_table.json
  [ ] 11.3 results/pareto_latency_vs_auroc.png (one per dataset)
  [ ] 11.4 results/pareto_params_vs_accuracy.png
  [ ] 11.5 step_writeups/step11.txt with RQ4 paragraph drafted

Exit criteria:
  [ ] efficiency_table.json complete for all MVT cells + baselines
  [ ] Two Pareto plots saved at 300 dpi
  [ ] RQ4 paragraph: numerical Pareto-optimal point
  [ ] Limitation noted: "no Jetson Nano access; CPU as edge proxy"

--------------------------------------------------------------------------------
STEP 12 — OPTIONAL EXTENSIONS                                   [ ] NOT STARTED
--------------------------------------------------------------------------------
Phase: 5     RQ: breadth only                         Est: variable

Drop order (priority top → bottom):
  [ ] 12.A ConvNeXt-Nano backbone (proposal §5A line 3)
  [ ] 12.B BitFit added to the MVT grid (already built in Step 5)
  [ ] 12.C CUB-200-2011 dataset (fine-grained)
  [ ] 12.D ISIC medical dataset (drop earliest if time tight)
  [ ] 12.E CIFAR-10-C corruption robustness (drop second)
  [ ] 12.F Baselines extended to MobileNetV3-Small and MiniImageNet

Exit criteria: per extension, a result JSON + one paragraph in step12.txt


================================================================================
PHASE 6 — THESIS WRITING  (NOT STARTED)
================================================================================

--------------------------------------------------------------------------------
STEP 13 — THESIS WRITING & SUBMISSION                           [ ] NOT STARTED
--------------------------------------------------------------------------------
Phase: 6                                              Est: 3-4 weeks

Actions:
  [ ] 13.1  Architecture diagram (TikZ or draw.io) for Chapter 3
  [ ] 13.2  Convert every result to LaTeX booktabs tables
  [ ] 13.3  Re-render every plot at 300 dpi, consistent fonts
  [ ] 13.4  Ch1 — Introduction (2-3 pp)
  [ ] 13.5  Ch2 — Literature Review (5-8 pp)
  [ ] 13.6  Ch3 — Methodology (8-12 pp); document the linear→prototype
            head deviation here
  [ ] 13.7  Ch4 — Experimental Setup (3-5 pp); reproducibility
            statement citing configs/test_episodes.yaml
  [ ] 13.8  Ch5 — Results (10-15 pp)
  [ ] 13.9  Ch6 — Discussion (3-5 pp); one subsection per RQ with
            explicit numerical answer + figure reference
  [ ] 13.10 Ch7 — Conclusion + Future Work (2-3 pp)
  [ ] 13.11 Abstract (250 words); BibTeX file alphabetised
  [ ] 13.12 Supervisor review pass; final revisions; SUBMIT

Exit criteria:
  [ ] All 7 chapters drafted
  [ ] Each RQ in Chapter 6 has a numerical answer + figure / table
      reference
  [ ] Supervisor signs off
  [ ] Document submitted


================================================================================
PROPOSAL COVERAGE — what plan + implementation deliver
================================================================================

  Proposal §                  Item                              Step / Status
  --------------------------  --------------------------------  -----------------
  §4 RQ1                      Adapter placement                 Step 6 (Phase 3)
  §4 RQ2                      Calibration evidential vs softmax Steps 1-10
  §4 RQ3                      Bayesian near-OOD detection       Step 7 (+ grid)
  §4 RQ4                      Latency / uncertainty Pareto      Step 11
  §5A Backbone: ResNet-18     mandatory                         Phase 1+ (done)
  §5A Backbone: MobileNetV3   mandatory                         Step 8
  §5A Backbone: ConvNeXt-Nano optional (drop order top)         Step 12.A
  §5A PEFT: Bottleneck        mandatory                         Phase 1+ (done)
  §5A PEFT: LoRA              mandatory                         Step 5
  §5A PEFT: BitFit            mandatory built; grid optional    Step 5 (+ 12.B)
  §5B Evidence activation     ReLU → softplus (documented dev.) Step 1 (done)
  §5B Head architecture       Linear → prototype (documented)   Step 4
  §5C KL anneal               implemented                       Phase 1+ (done)
  §6 Dataset: CIFAR-FS        mandatory                         Step 4
  §6 Dataset: MiniImageNet    mandatory                         Step 9
  §6 Dataset: CUB-200         optional (drop order)             Step 12.C
  §6 Dataset: ISIC            optional (drop earliest)          Step 12.D
  §6 OOD: SVHN                done (Phase 1)                    -
  §6 OOD: TinyImageNet        mandatory                         Step 7
  §6 OOD: Gaussian noise      mandatory                         Step 7
  §6 OOD: CIFAR-10-C          optional (drop order)             Step 12.E
  §6 Baseline: Full FT        mandatory                         Step 5
  §6 Baseline: Linear Probe   mandatory                         Step 5
  §6 Baseline: Std LoRA+Soft  covered by Step 5 LoRA softmax    Step 5
  §7 Metric: Top-1 Accuracy   done                              Phase 1+ (done)
  §7 Metric: Macro-F1         GAP -- added in Step 4 evaluator  Step 4 (4.7.1)
  §7 Metric: ECE              done                              Phase 1+ (done)
  §7 Metric: Brier            done                              Phase 1+ (done)
  §7 Metric: AUROC            done                              Phase 1+ (done)
  §7 Metric: FPR@95           added in Step 4 evaluator         Step 4 (4.7)
  §7 Metric: Trainable params done                              Phase 1+ (done)
  §7 Metric: GPU mem / FLOPs  added                             Step 11
  §7 Metric: Latency          added                             Step 11


================================================================================
NOTES / DECISIONS LOG
================================================================================

2026-04-21  Decision (Step 1): use softplus activation instead of ReLU for
            evidence computation. Reason: ReLU caused dead neurons (negative
            logit → zero evidence → zero gradient → unrecoverable). Logged
            in step1.txt sections 2-3.

2026-04-21  Decision (Step 1): KL anneal 0 → 0.5 over 200 steps. Reason: on
            25-sample support sets a fixed kl_weight=0.5 overwhelms the MSE
            signal and the model gets stuck at uniform predictions; ramp
            gives MSE time to grow evidence before the prior kicks in.

2026-05-13  Decision (Step 2): YAML configs with `extends:` over a Python
            dataclass. Reason: the MVT grid will need ~120+ small configs;
            each must be diff-readable and runnable from the CLI without
            editing source.

2026-05-16  Step 3 ran wandb in OFFLINE mode (not online). Reason: embedded
            API key was 36 characters; wandb requires 40+. The `wandb/`
            directory is sync-ready. Resolve before Step 10 starts (the
            MVT grid wants online tracking for cross-run comparison).

2026-05-19  Decision (plan.txt, Step 4): episodic meta-training, NOT
            per-episode-from-scratch. Reason: supervisor_briefing §6
            commits to "train on many episodes per epoch, validate on
            others, stop when validation plateaus" — that is meta-training.

2026-05-19  Decision (plan.txt, Step 4): 5-shot training only for Phase 2;
            1-shot deferred to the MVT grid (Step 10). Reason: Phase 2's
            §4.5 spec calls for a single Phase A baseline (two configs:
            evidential + softmax), not a grid.

2026-05-19  Decision (implementation.txt, Step 4): prototype-head meta-
            training instead of the proposal's linear head. Reason:
            classifier head trained over 64 train classes cannot
            transfer to 20 test classes; prototype similarity is the
            standard CIFAR-FS protocol. Documented as a methodology
            deviation in Chapter 3 of the thesis.

2026-05-19  Audit gap fix: Macro-F1, Full Fine-Tuning baseline, and Linear
            Probing baseline added to plan.txt and implementation.txt.
            They were missing from the original post-defence spec but
            are mandatory per proposal §6 and §7.

2026-07-08  Decision (Step 5, LoRA): target the 1x1 downsample conv of the
            final stage (layer4.0.downsample.0) — a pure channel matrix, so
            LoRA on it is the clean rank*(in+out) reparameterization, 12,288
            params @ rank 16 (inside the 9-15K band, comparable to Bottleneck).
            A 3x3 conv2 target would explode to >160K params (down-projection
            carries the full k*k kernel). Target set is a config knob
            (cfg.adapter.lora_targets) so it can be widened later.

2026-07-08  Flag (Step 5): implementation.txt 5.1's LoRA param formula
            "2*rank*(in+out)" is wrong by 2x; the honest A/B count is
            rank*(in+out). Tests assert the correct value; noted in step5.txt.

2026-07-08  Decision (Step 5, BatchNorm): for ALL backbone-trainable adapters
            (LoRA/BitFit/Full-FT) the trainer keeps the backbone in eval() mode,
            so BN running stats stay frozen at ImageNet values; only affine/conv
            weights update. Updating BN stats from 25-image supports is unstable
            and leaks query stats. Documented deviation from vanilla Full-FT.

2026-07-08  Decision (Step 5, Full-FT): its config sets collapse_threshold=0.0,
            which disables the R-EPISODIC-COLLAPSE guard (now gated on
            threshold>0), so the EXPECTED overfit/collapse completes and writes
            honest metrics instead of aborting the Colab run. LR 1e-5, wd 1e-4.

2026-07-26  Decision (Step 8, feature dim): MobileNetV3-Small's frozen feature is
            the 576-d POOLED CONV feature (torchvision classifier -> Identity),
            per implementation.txt 8.1. Flagged alternative NOT taken: the paper
            moves the final feature expansion after pooling, which in torchvision
            is classifier[0] (Linear 576->1024), so a 1024-d feature is available.
            Rejected because the spec says 576, classifier[0] is a 1000-class-tuned
            layer, and the smaller feature is the honest "small backbone" for RQ4.

2026-07-26  Decision (Step 8, placement sites): in-block adapter stages are
            DERIVED from the backbone, not hardcoded — the stage-final RESIDUAL
            inverted-residual block of each channel-width group
            (features.3/6/8/11 = 24/40/48/96 ch for V3-Small). Only residual
            blocks (stride 1, in==out) qualify, because parallel composition adds
            body(block_input) to the block output and needs matching shapes.
            Consequence to remember: V3-Small's first downsampling group
            (features.1) is stride-2, so it has NO eligible site — the 4 mbnet
            sites are "analogous depth", not the same resolutions as ResNet-18's
            layer1..layer4. Escape hatch: cfg.adapter.stage_paths.

2026-07-26  Result (Step 8, run): the ~7-8 pp accuracy drop that §0 predicted from
            MobileNetV3-Small's ImageNet top-1 deficit did NOT materialise — the
            measured gap at matched placement is 0.3-1.6 pp (and the two softmax
            gaps are inside the 600-episode CI95). NB the "~7-8 pp" expectation
            itself is general knowledge, not summary-sourced: the summaries state
            only V3-Small = ~67.4-67.5% top-1, never ResNet-18's — flagged in
            step8.txt §4.3(b). Conclusion to carry into
            Chapter 5: on 5-way 5-shot CIFAR-FS with a prototype head, ImageNet
            top-1 is a poor predictor of few-shot transfer, so the RQ4 efficiency
            axis costs far less accuracy than the backbone-size ratio suggests.
            Confound that must be stated with it: the recipe was inherited from a
            ResNet-18 VAL sweep and never re-tuned for this backbone, which if
            anything biases AGAINST MobileNetV3 — so 0.3-1.6 pp is an upper bound
            on the cost, not a tuned optimum.

2026-07-26  Result (Step 8, RQ1): the Step-6 placement conclusion strengthens on
            the second backbone. In-block parallel beats post_pool by +3.7 pp on
            BOTH heads while costing 2.75x FEWER params, i.e. strictly
            Pareto-dominant here (on ResNet-18 the same win cost 1.88x more).
            Combined with the param-ordering flip logged below, the honest RQ1
            statement is "in-block parallel wins on accuracy on both backbones;
            whether it also wins on params depends on the backbone's channel
            profile" — not "parallel costs ~2x params".

2026-07-26  Finding to flag, NOT to bury (Step 8, RQ3): energy beats evidential
            vacuity on 3 of 4 OOD pools on BOTH MobileNetV3 placements
            (parallel: gaussian −0.048, cifar100 −0.013, tin −0.036; svhn +0.004
            to vacuity). On ResNet-18/parallel the same comparison was near-even
            (2-2). The claim that survives the backbone swap intact is the
            PROBABILISTIC one — vacuity > MSP and > TS-MSP on all 8 cells, by
            +0.015 to +0.170. Steps 4.5/7's "on par with energy" line must be
            scoped to ResNet-18 wherever it appears in the thesis text.

2026-07-26  Open question (Step 8 -> Step 10): mbnet/parallel evidential has the
            worst evidential ECE in the thesis (0.3069) and is the only evidential
            run whose checkpoint was selected DURING KL warm-up (epoch 3,
            kl_w=0.030 of a 0.100 plateau; val-acc peaked there and patience 5
            stopped it at epoch 8). Prime suspect, untested. Step 10's VAL tuning
            should try a longer warm-up and/or patience > 5 before any claim is
            made about the loss on this backbone. Do not cite 0.3069 as a
            property of the evidential loss.

2026-07-26  Observation (Step 8, RQ4 relevance): on MobileNetV3 the param
            ordering FLIPS vs ResNet-18 — in-block parallel is ~2.7x CHEAPER than
            the post-pool adapter (6,928 vs 19,024), because stage widths are
            24-96 ch while the pooled feature is 576-d. On ResNet-18 parallel cost
            ~1.9x MORE (31,744 vs 16,912). Same adapter form, opposite verdict:
            the param cost of a placement is a property of the backbone's channel
            profile, which is a point Chapter 5 should make explicitly.

2026-07-26  Decision (Step 8, reproducibility): Steps 1-7 must reproduce
            unchanged, so every Step 8 edit is additive with the old default
            preserved: build_frozen_resnet18/build_backbone gained
            pretrained=True (default = old behaviour); the placement/LoRA
            refactors resolve to the SAME ResNet modules/targets (3 regression
            tests assert it); cfg.output.run_tag defaults to null, reproducing
            the old checkpoint filenames; and the metrics-JSON schema was left
            UNTOUCHED on purpose — adding a field would make every re-run of a
            Step 4.5/5/6/7 config differ from its committed result file. Anything
            a new step needs about a run (backbone, placement) is derived from the
            result FILENAME by the consolidation script instead.

2026-07-08  Architecture (Step 5): LoRA/BitFit/Full-FT put trainable params
            INSIDE the frozen backbone. build_adapter now receives the backbone
            and mutates it in place; the model exposes backbone_trainable so
            adapter_features runs the backbone WITH autograd (vs no_grad for the
            frozen Bottleneck/Linear-Probe/legacy paths). The single_episode +
            Bottleneck paths stay byte-identical. Linear-Probe+softmax has 0
            trainable params -> train.py short-circuits to a train-free
            checkpoint (Adam rejects an empty parameter list).

2026-07-27  Decision (Step 9, data source): MiniImageNet images come from the
            learn2learn Zenodo record 7978538 (mini-imagenet-cache-{train,
            validation,test}.pkl), verified live during planning to serve over
            plain HTTPS with published MD5s — no auth, no gdown. Reason:
            cheapest reliable no-auth source found; a Kaggle-attached dataset
            (any of 3 layouts: the same pkls, images/+csv, or a wnid
            imagefolder) is auto-discovered first and preferred when present.

2026-07-27  Decision (Step 9, split materialization): mirror CIFAR-FS —
            regenerate into gitignored data/ via scripts/build_mini_imagenet_
            split.py, rather than committing the split JSON to git. Reason:
            consistency with the existing CIFAR-FS convention; the 100-wnid
            split is additionally frozen as a Python constant
            (MINI_IMAGENET_SPLIT in src/datasets/mini_imagenet.py), so it is
            in git as code even though the generated JSON is not, and offline
            structural tests need no network access.

2026-07-27  Decision (Step 9, OOD pools): TinyImageNet-200 shares 25 wnids
            with MiniImageNet's 100 classes — 5 of them MiniImageNet TEST
            classes (verified by diffing the two wnid lists during planning).
            Unfiltered, ~2.5% of the "tin_near" OOD pool would be literal
            in-distribution images, biasing RQ3's headline metric. Fix:
            get_tinyimagenet_ood gained exclude_wnids (default None, byte-
            identical to every earlier step); evaluate.py passes
            MINI_IMAGENET_ALL_WNIDS (all 100, not just the 20 test wnids)
            only when cfg.dataset.name == "mini_imagenet". Consequence: Step
            9's tin_near numbers are NOT directly comparable to Steps 4.5/7/8's
            tin_near (different 500-image sample) — flag this if the two are
            ever put in the same table column.

2026-07-27  Decision (Step 9, run scope): 10 runs, not the spec's 2-run
            minimum — user asked for maximum research value. Beyond
            {ResNet-18, parallel} x {evidential, softmax} (the literal spec),
            added: the ResNet-18 post_pool sibling (RQ1 external validity on
            a 2nd dataset), the MobileNetV3-Small post_pool/parallel pair
            (tests whether Step 8's "energy beats vacuity on MobileNetV3"
            finding is a backbone property or a CIFAR-FS artifact, and gives
            a less ImageNet-saturated 2nd cell), and a ResNet-18 linear-probe
            reference pair (Chen 2019 "A Closer Look" motivates this as the
            way to MEASURE, not just caveat, the ImageNet-pretraining-overlap
            confound: parallel_acc - linear_probe_acc is the adapter's uplift
            over raw frozen features, vs the already-committed CIFAR-FS
            uplift of +4.1pp).

2026-07-27  Architecture (Step 9): get_id_split(dataset_cfg, split) and
            get_heldout_near_ood(dataset_cfg, ...) added to
            src/datasets/__init__.py, replacing 6 hardcoded get_cifar_fs(...)
            call sites in scripts/train.py (2), scripts/evaluate.py (3), and
            scripts/step45_val_sweep.py (1) (cfg.dataset.name was previously
            dead on the episodic path — only the legacy build_dataset()
            factory consulted it). Every pre-Step-9 config never sets
            dataset.name, so both functions default to the cifar_fs branch
            with the exact same positional/keyword arguments as the old
            direct calls — proven byte-identical by monkeypatched regression
            tests in tests/test_dataset_dispatch.py, plus 2 real
            (non-mocked) tests comparing routed vs direct get_cifar_fs output.
            get_heldout_near_ood also returns the pool NAME (cifar100_near /
            mini_near) so evaluate.py's OOD-pool dict needs no dataset
            conditional — the evaluator already generates summary keys
            dynamically per pool name.

2026-07-27  Status note (Step 9, historical — superseded by the 2026-07-30
            entries below): this step was implemented in a session with no
            GPU, no torch/numpy/PIL/torchvision, and no package manager
            available (verified before starting) — every file was written,
            syntax-checked (python -m py_compile across the whole repo) and,
            for configs, validated through the real load_config deep-merge;
            none of it had been executed against real data yet.
            step_writeups/step9.txt §4 was, at this point, an explicit
            template, not a result.

2026-07-30  Result (Step 9, run): the 10-config Kaggle T4 run completed
            (00:54:55-03:17 UTC, ~142 min, 10/10 OK, no collapse). The
            Zenodo pkl schema assumption flagged 2026-07-27
            (_load_pkl_cache) decoded 3 real staged pkls correctly (test
            20/train 64/val 16 classes, all "OK") — CLOSED, no longer a
            caveat. The Zenodo-403/user_agent download fix from the same
            entry was NOT exercised end-to-end this run (pkls arrived
            pre-staged via the attached Kaggle dataset, so
            ensure_archive()'s download path was never called) — remains
            code-review-verified only, not execution-verified.

2026-07-30  Result (Step 9, RQ2/RQ3): the ImageNet-pretraining-overlap
            confound flagged at design time is now MEASURED: the
            parallel-minus-linear-probe accuracy uplift shrinks from
            CIFAR-FS's +3.84 pp (softmax) / +4.05 pp (evidential) to
            MiniImageNet's +1.39 pp / +1.90 pp. Evidential vacuity beats
            softmax MSP/TS-MSP on all 16 of 16 MiniImageNet (pool, config)
            comparisons (the probabilistic claim now survives both the
            Step 8 backbone swap and this dataset swap), but against ENERGY
            it wins only 7 of 16 — a further erosion of the "on par with
            energy" line, which should now be scoped to CIFAR-FS/ResNet-18
            specifically wherever it appears in the thesis text, not just
            "ResNet-18" as Step 8 concluded. Evidential ECE is essentially
            flat across BOTH datasets (0.267-0.318 here vs 0.264-0.307 on
            CIFAR-FS) while TS-softmax ECE reaches new thesis-wide lows on
            MiniImageNet (0.0047-0.0053, ResNet-18) — pushing the
            evidential-vs-TS-softmax gap to 55-57x, the strongest evidence
            yet for Step 4.5's "the ECE surface is flat" conclusion.

2026-07-30  Finding to flag, NOT to bury (Step 9, RQ4-adjacent, unexpected):
            Step 8's backbone-cost finding (MobileNetV3 costs only 0.3-1.6
            pp vs ResNet-18 on CIFAR-FS) does NOT replicate on MiniImageNet
            — the same gap there is 4.7-5.7 pp, 2.9-19x larger and outside
            CI95. Nothing in Step 9's paper-grounded reasoning (step9.txt §0)
            predicts this; it was written before any MiniImageNet number
            existed. Two untested candidate explanations (backbone-capacity
            interaction with the pretraining-overlap confound vs. the
            untuned-recipe caveat hitting MobileNetV3 harder here) are named
            in step9.txt §4.3e but NOT adjudicated — Step 10's per-cell VAL
            tuning is what will distinguish them. Do not repeat "MobileNetV3
            is almost free" as a dataset-independent claim.

2026-07-30  Open question (Step 8 -> Step 9 -> Step 10, now 2-for-2):
            mbnet/parallel evidential's checkpoint-selected-during-KL-
            warm-up pattern (Step 8: epoch 3/30, kl_w=0.030, worst
            evidential ECE 0.3069 on CIFAR-FS) REPRODUCED on MiniImageNet
            (epoch 6/30, kl_w=0.060, again the worst evidential ECE of its
            dataset, 0.3176). Two independent datasets now show the same
            cell hitting the same failure mode — this raises the priority
            of Step 10 testing a longer KL warm-up / patience>5 specifically
            for mbnet/parallel, though it remains a plausible, UNTESTED
            explanation, not a proven cause.

2026-07-30  Caveat (Step 9, test suite, traced not just flagged): the full
            pytest suite on the Step 9 Kaggle session returned 208 passed /
            6 failed (214 collected, more than the ~170 pre-run estimate —
            the estimate counted test FILES, not post-parametrization pytest
            nodes). All 6 failures trace to ONE root cause: this session had
            a real, persistent Kaggle dataset attached under /kaggle/input,
            which shadows tests written assuming nothing is staged. 5 of 6
            predate Step 9 (test_cifar_fs.py x4, test_svhn_ood.py x1). The
            6th, new in Step 9
            (test_tinyimagenet_ood.py::test_exclude_wnids_drops_the_named_
            classes), fails via the identical mechanism:
            _find_extracted_tin_root's deliberate /kaggle/input recursive
            search finds a real, attached 200-class TinyImageNet before the
            test's synthetic tmp_path fixture, so the test's fake wnids
            match nothing real and exclude nothing. Traced by code
            inspection to be a TEST-ISOLATION bug, not a defect in the
            exclude_wnids filtering logic itself (the real evaluate.py path
            filters real wnids against real wnids, which should collide
            correctly) — but this conclusion is NOT independently confirmed
            by a logged exclusion count from the real run; the notebook's
            overlap-report cell ran too early (before TinyImageNet data was
            touched) and was never re-run. Full trace: step_writeups/
            step9.txt §5. Recommended fix for whoever revisits this: add a
            one-line kept/excluded print inside get_tinyimagenet_ood, and
            isolate the test from /kaggle/input (monkeypatch or use wnids
            that collide with a real synset).

2026-07-30  Reproducibility note (Step 9): byte-identical rerun spot-check
            is STILL NOT DONE — carried over from Step 8 (never closed
            there either) and now also blocked structurally: the 10
            checkpoints from this run are listed (with sha256) in
            results/step9_mini_artifacts/MANIFEST.txt but were NOT
            committed (checkpoints/ is gitignored), so this git checkout
            cannot do the spot-check without either a fresh Kaggle run or
            recovering the pushed-checkpoint artifact.


================================================================================
END OF PROGRESS.TXT
================================================================================
