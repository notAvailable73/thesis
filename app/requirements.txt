# Sentinel application dependencies (in addition to the thesis requirements.txt).
# torch/torchvision are shared with the research code; pinned loosely for CPU use.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=9.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
